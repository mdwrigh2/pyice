var a: int;
var b: int;
var c: int;
a := 10;
b := 20;
c := 30;
write ? (1 < 1);
write ? (1 > 2);
write ? (1 > 1);
write ? (true != false);
write ? (false = false);
write ? (true = true);
write ? (1 != 2);
write ? (1 = 1);
write ? (1 < 2);
write a; ## var
write b / 2; ## var
write c + -a * 2; ## exp
write c - a; ## exp
write c * a / 15; ## exp
write c / a + 17; ## exp
write c / a * a;
write c / a * a;
write c / a * a - b + (c * a / 15);
write c / a * a - b * c + 470;
