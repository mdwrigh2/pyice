proc g(b: int)
  var a : int;
  a:=0;
  fa i := 1 to b ->
    a := a+i;
  af
  writes "Sum of 1 to ";
  writes b;
  writes ": ";
  write a;
end

proc f()
  fa i := 1 to 20 ->
    g(i);
  af

end

f();
