#:compile
var x : int;
proc x(x, y : int)
  fa x := x to y ->
    fa x := x/2 to (x-1)/2 ->
      fa x := ?(x < 1) to 0 ->
        write x;
      af
      write x;
    af
    write x;
  af
  write x;
end
x(3, 1);
x(1, 3);
x(1, 1);
